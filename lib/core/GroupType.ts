export type GroupType = "quad" | "triple" | "double" | "sequence" | "special";
export const GroupType = {
  QUAD: "quad" as GroupType,
  TRIPLE: "triple" as GroupType,
  DOUBLE: "double" as GroupType,
  SEQUENCE: "sequence" as GroupType,
  SPECIAL: "special" as GroupType,
}
